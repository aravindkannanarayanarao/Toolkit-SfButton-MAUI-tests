<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SfButtonSample.Features.Properties"
    xmlns:busy="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
    xmlns:buttons="clr-namespace:Syncfusion.Maui.Toolkit.Buttons;assembly=Syncfusion.Maui.Toolkit"
    xmlns:local="clr-namespace:SfButtonSample.ViewModel"
             Title="Properties">

    <ContentPage.BindingContext>
        <local:ButtonViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid RowDefinitions="Auto,*">
            <VerticalStackLayout Margin="30">
                <buttons:SfButton
                    Text="{Binding Text}"
                    Background="{Binding Background}"
                    TextColor="{Binding TextColor}" 
                    FontFamily="{Binding FontFamily}"
                    HeightRequest="{Binding ButtonHeight}"
                    WidthRequest="{Binding ButtonWidth}"
                    FlowDirection="{Binding FlowDirection}"
                    IsVisible="{Binding IsVisible}"
                    IsEnabled="{Binding IsEnabled}"
                    StrokeThickness="{Binding BorderThickness}"
                    ImageAlignment="{Binding ImageAlignment}"
                    ShowIcon="{Binding ShowIcon}"
                    ImageSource="{Binding ImageSource}"
                    Padding="{Binding PaddingSlider}" AutomationId="tes" SemanticProperties.Description="tes" />
            </VerticalStackLayout>
            
            <StackLayout Grid.Row="1">
                <Grid ColumnDefinitions="*,*">
                    <Grid Grid.Column="0"  ColumnDefinitions="*,*,*" RowDefinitions="*,*,*,*,*,*,*,*">
                        <Label Grid.Row="0" Text="Enter any Name" />
                        <Entry Grid.Row="0" Grid.Column="1" Text="{Binding Text}" AutomationId="name" />

                        <Label Text="Font Family" Grid.Row="1"></Label>
                        <Picker Grid.Row="1" Grid.Column="1" ItemsSource="{Binding FontFamilyList}" SelectedIndex="{Binding FontFamilySelectedIndex}" AutomationId="Family"></Picker>

                        <Label Grid.Row="2" Text="Text Color" />
                        <Picker Grid.Row="2" Grid.Column="1" ItemsSource="{Binding TextColorList}" SelectedIndex="{Binding TextColorSelectedIndex}" AutomationId="TextColor" />

                        <Label Grid.Row="3" Text="Button Height"/>
                        <!--//<Slider Grid.Row="3" Grid.Column="1" HorizontalOptions="Start" Maximum="300" Minimum="100" WidthRequest="100" Value="{Binding ButtonHeight}" AutomationId="btnheighslider"/>-->
                        <Entry Grid.Row="3" Grid.Column="1" HorizontalOptions="Start" Text="{Binding ButtonHeight}" AutomationId="btnheighslider" SemanticProperties.Description="btnheighslider"/>
                        
                        
                        <Label Grid.Row="4" Text="Button Width"/>
                        <!--<Slider Grid.Row="4" Grid.Column="1" HorizontalOptions="Start" Maximum="300" Minimum="100" WidthRequest="100" Value="{Binding ButtonWidth}" AutomationId="btnwidtslider"/>-->
                        <Entry Grid.Row="4" Grid.Column="1" HorizontalOptions="Start" Text="{Binding ButtonWidth}" AutomationId="btnwidtslider"/>


                        <Label Text="FlowDirection" Grid.Row="5"/>
                        <Picker  Grid.Row="5" Grid.Column="1" ItemsSource="{Binding FlowDirectionList}" SelectedIndex="{Binding FlowDirectionSelectedIndex}" AutomationId="direction"/>
                    </Grid>
                    <Grid Grid.Column="1" ColumnDefinitions="*,*,*" RowDefinitions="*,*,*,*,*,*,*,*">
                    <Label Text="IsEnabled" Grid.Row="0"/>
                    <Switch Grid.Row="0" Grid.Column="1" IsToggled="{Binding IsEnabled}" AutomationId="IsEnab"/>

                    <Label Text="IsVisible" Grid.Row="1"></Label>
                    <Switch Grid.Row="1" Grid.Column="1" IsToggled="{Binding IsVisible}" AutomationId="isvis"/>
                    
                    <Label Grid.Row="2" Text="Button Thickness"/>
                    <!--<Slider Grid.Row="2" Grid.Column="1" HorizontalOptions="Start" Maximum="20" Minimum="0" WidthRequest="100" Value="{Binding BorderThickness}" AutomationId="bslider"/>-->
                    <Entry Grid.Row="2" Grid.Column="1" HorizontalOptions="Start" Text="{Binding BorderThickness}" AutomationId="bslider"/>

                    <Label Grid.Row="3" Text="ShowIcon"/>
                    <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding ShowIcon}" AutomationId="Showic"/>

                    <Label Grid.Row="4" Text="ImageSource"/>
                    <Picker Grid.Row="4" Grid.Column="1" ItemsSource="{Binding ImageSourceList}" SelectedIndex="{Binding ImageSourceSelectedIndex}" AutomationId="imagesource" />

                    <Label Grid.Row="5" Text="Padding" />
                    <!--<Slider Grid.Row="5" Grid.Column="1" HorizontalOptions="Start" Maximum="15" Minimum="0" WidthRequest="100" Value="{Binding PaddingSlider}" AutomationId="paddingslider"/>-->
                    <Entry Grid.Row="5" Grid.Column="1" HorizontalOptions="Start" Text="{Binding PaddingSlider}" AutomationId="paddingslider"/>
                </Grid>
                </Grid>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
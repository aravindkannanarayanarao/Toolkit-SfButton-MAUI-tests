<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:listview="clr-namespace:Syncfusion.Maui.ListView;assembly=Syncfusion.Maui.ListView"
             xmlns:expander="clr-namespace:Syncfusion.Maui.Expander;assembly=Syncfusion.Maui.Expander"
             xmlns:buttons="clr-namespace:Syncfusion.Maui.Core;assembly=Syncfusion.Maui.Core"
             x:Class="AccordionSample.MAUI_30507"
             Title="MAUI_30507">

    <ContentPage.Content>
        <StackLayout Margin="0,40,0,0">
            <listview:SfListView ItemsSource="{Binding Items}" AutoFitMode="DynamicHeight" SelectedItem="{Binding SelectedItem}">
                <listview:SfListView.ItemTemplate>
                    <DataTemplate>
                        <expander:SfExpander IsExpanded="True" >
                            <expander:SfExpander.Header>
                                <Grid>
                                    <Label Text="{Binding Name}"/>
                                </Grid>
                            </expander:SfExpander.Header>

                            <expander:SfExpander.Content>

                                <buttons:SfChipGroup DisplayMemberPath="Name" ItemsSource="{Binding MoreItems}">
                                    <buttons:SfChipGroup.ChipLayout>
                                        <FlexLayout
                              HorizontalOptions="Start"
                              VerticalOptions="Start"
                              Direction="Row"
                              Wrap="Wrap"
                              JustifyContent="Start"
                              AlignContent="Start"
                              AlignItems="Start"/>
                                    </buttons:SfChipGroup.ChipLayout>
                                </buttons:SfChipGroup>

                            </expander:SfExpander.Content>

                        </expander:SfExpander>
                    </DataTemplate>
                </listview:SfListView.ItemTemplate>


            </listview:SfListView>
            <Button Text="Add chip to selected" Clicked="Button_OnClicked" VerticalOptions="End"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>